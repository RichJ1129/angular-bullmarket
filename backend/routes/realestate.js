const express = require("express");
const jwt = require("jsonwebtoken");
const RealEstate = require("../models/country");
const router = express.Router();

//router.get("",(req, res) => {
///  console.log("Real Estate Route")
  //Country.find((error, data) => {
  //  if (error) {
  //    return next(error);
  //  } else {
  //    res.json(data);
  //  }
 // })
//});